{
  "name": "prediqt",
  "version": "2.0.0",
  "private": true,
  "description": "PredIQT monorepo (FastAPI backend + React Native mobile app)",
  "scripts": {
    "mobile:start": "npm --prefix mobile-native-app run start",
    "mobile:android": "npm --prefix mobile-native-app run android",
    "mobile:ios": "npm --prefix mobile-native-app run ios"
  }
}
